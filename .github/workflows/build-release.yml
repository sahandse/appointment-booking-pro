name: Build Plugin ZIP

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare plugin
        run: |
          mkdir -p build/appointment-booking-pro
          rsync -av --exclude='.git' --exclude='.github' --exclude='build' ./ build/appointment-booking-pro/

      - name: Create ZIP
        run: |
          cd build
          zip -r appointment-booking-pro.zip appointment-booking-pro

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: appointment-booking-pro
          path: build/appointment-booking-pro.zip

      - name: Publish GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: build/appointment-booking-pro.zip
          generate_release_notes: true
